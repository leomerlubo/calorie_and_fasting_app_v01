import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.selfloveapp",
  appName: "selflove-app",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
